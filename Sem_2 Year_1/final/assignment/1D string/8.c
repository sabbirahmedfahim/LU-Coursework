// Write a C Program to Search and Replace a word with a Specific Word
// [problem] 
#include <stdio.h>

int main()
{
    // char arr[101];
    // char specificWord[101];
    // scanf("%s", arr);
    // scanf("%s", specificWord);

    for (int i = 0; i < 5 - 1; i++)
    {
        for (int j = i + 1; j < 5; j++)
        {
            printf("%d - %d\n", i, j);
        }
    }

    return 0;
}