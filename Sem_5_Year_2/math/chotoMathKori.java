class doSomeMath
{
    public static void main(String[] args) 
    {
        int x = 10, y = 20;
        System.out.println(Math.max(x, y));
        System.out.println(Math.min(x, y));

        /*
        Math.sqrt(x,y);
        Math.random(x,y);
        Math.abs(x,y);
        */
    }
}